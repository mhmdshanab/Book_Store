<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>User Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

  <% if (!user) { %>
    <div class="container mt-5 text-center">
      <div class="alert alert-danger">❌ Access Denied. Please log in first.</div>
      <a href="/login" class="btn btn-primary">🔐 Login</a>
    </div>
  <% } else { %>

    <div class="container mt-5 text-center">
      <h2 class="text-primary">👋 Welcome, <%= user.username %>!</h2>
      <p class="lead">Browse books, manage your cart, and enjoy reading!</p>

      <div class="mt-4">
        <a href="/books/allBooks" class="btn btn-outline-success me-2">📚 View All Books</a>
        <a href="/cart/allCart" class="btn btn-outline-warning me-2">🛒 Your Cart</a>
        <a href="/logout" class="btn btn-outline-danger">🚪 Logout</a>
      </div>
    </div>

  <% } %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
