<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>✅ Order Confirmation</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <%- include('partials/header') %>

  <div class="container my-5">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card border-success shadow">
          <div class="card-header bg-success text-white text-center">
            <h3><i class="fas fa-check-circle me-2"></i>Order Confirmed!</h3>
          </div>
          <div class="card-body">
            <div class="text-center mb-4">
              <i class="fas fa-shopping-bag text-success" style="font-size: 4rem;"></i>
            </div>
            
            <h4 class="text-success text-center mb-4">Thank you for your purchase!</h4>
            
            <div class="row">
              <div class="col-md-6">
                <h5 class="text-primary">📋 Order Details</h5>
                <ul class="list-unstyled">
                  <li><strong>Order ID:</strong> <%= order.id %></li>
                  <li><strong>Date:</strong> <%= new Date().toLocaleDateString() %></li>
                  <li><strong>Status:</strong> <span class="badge bg-success">Paid</span></li>
                  <li><strong>Total:</strong> $<%= order.total %></li>
                </ul>
              </div>
              
              <div class="col-md-6">
                <h5 class="text-primary">👤 Customer Info</h5>
                <ul class="list-unstyled">
                  <li><strong>Name:</strong> <%= user.username %></li>
                  <li><strong>Email:</strong> <%= user.email || 'N/A' %></li>
                </ul>
              </div>
            </div>
            
            <hr class="my-4">
            
            <h5 class="text-primary">📚 Purchased Items</h5>
            <div class="table-responsive">
              <table class="table table-striped">
                <thead>
                  <tr>
                    <th>Book</th>
                    <th>Quantity</th>
                    <th>Price</th>
                    <th>Total</th>
                  </tr>
                </thead>
                <tbody>
                  <% order.items.forEach(item => { %>
                    <tr>
                      <td><%= item.title %></td>
                      <td><%= item.quantity %></td>
                      <td>$<%= item.price %></td>
                      <td>$<%= (item.price * item.quantity).toFixed(2) %></td>
                    </tr>
                  <% }) %>
                </tbody>
              </table>
            </div>
            
            <div class="text-center mt-4">
              <a href="/books/allBooks" class="btn btn-primary me-2">
                <i class="fas fa-book me-1"></i>Continue Shopping
              </a>
              <a href="/dashboard/home" class="btn btn-secondary">
                <i class="fas fa-home me-1"></i>Go to Dashboard
              </a>
            </div>
            
            <div class="alert alert-info mt-4">
              <i class="fas fa-info-circle me-2"></i>
              <strong>Note:</strong> You will receive a confirmation email shortly. 
              Keep this order ID for your records.
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
