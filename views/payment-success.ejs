<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>✅ Payment Successful</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <%- include('partials/header') %>

  <div class="container my-5">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card border-success">
          <div class="card-header bg-success text-white text-center">
            <h3>✅ Payment Successful!</h3>
          </div>
          <div class="card-body text-center">
            <div class="mb-4">
              <i class="fas fa-check-circle text-success" style="font-size: 4rem;"></i>
            </div>
            
            <h4 class="text-success mb-3">Thank you for your purchase!</h4>
            <p class="lead">Your order has been processed successfully.</p>
            
            <% if (session) { %>
              <div class="alert alert-info">
                <strong>Order Details:</strong><br>
                <strong>Order ID:</strong> <%= session.id %><br>
                <strong>Amount:</strong> $<%= (session.amount_total / 100).toFixed(2) %><br>
                <strong>Status:</strong> <%= session.payment_status %>
              </div>
            <% } %>
            
            <div class="mt-4">
              <a href="/books/allBooks" class="btn btn-primary me-2">📚 Continue Shopping</a>
              <a href="/dashboard/home" class="btn btn-secondary">🏠 Go to Dashboard</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 