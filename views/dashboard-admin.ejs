<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Admin Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

  <% if (!user || !user.isAdmin ) { %>
    <div class="container mt-5 text-center">
      <div class="alert alert-danger">❌ Access Denied. Admins only.</div>
      <a href="/login" class="btn btn-primary">🔐 Login</a>
    </div>
  <% } else { %>

    <div class="container mt-5 text-center">
      <h2 class="text-success">🔧 Admin Panel - <%= user.username %></h2>
      <p class="lead">Manage books, view library data, and monitor activity.</p>

      <div class="mt-4">
        <a href="/books/allBooks" class="btn btn-outline-primary me-2">📚 Manage Books</a>
        <a href="/books/add-book" class="btn btn-outline-success me-2">➕ Add New Book</a>
        <a href="/logout" class="btn btn-outline-danger">🚪 Logout</a>
      </div>
    </div>

  <% } %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
